# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*
.Spotlight-V100
.Trashes

# Xcode
build/
DerivedData/
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
xcuserdata/
*.xccheckout
*.moved-aside
*.xcuserstate
*.xcscmblueprint

# Generated Xcode project (xcodegen output — see project.yml)
*.xcodeproj
*.xcworkspace

# Swift Package Manager
.build/
Packages/
Package.pins
Package.resolved
.swiftpm/

# CocoaPods (we don't plan to use these but just in case)
Pods/

# Carthage
Carthage/Build/

# fastlane
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots
fastlane/test_output

# Code coverage
*.gcno
*.gcda
*.profraw
*.profdata

# IDE noise
.idea/
.vscode/
*.swp
*.swo

# Local config / secrets (never commit)
.env
.env.local
secrets/

# Node (if we ever add a tooling script)
node_modules/

# App data dumped during dev
debug-snapshots/
*.html.snapshot

# Vendored Chrome for Testing (downloaded by scripts/fetch-chrome-for-testing.sh
# during build; not committed because each arch is ~340 MB). The Xcode build
# phase invokes the fetch script automatically when this directory is empty.
vendor/chrome-for-testing/
